modules:
    - type: rpm-ostree
      install:
        - libvirt
        - qemu-kvm
        - NetworkManager
    - type: containerfile
      snippets:
        - ADD https://developers.redhat.com/content-gateway/rest/mirror/pub/openshift-v4/clients/crc/latest/crc-linux-amd64.tar.xz /usr/bin/crc-linux-amd64.tar.xz
    # - type: script
    #   scripts:
    #     - installcrc.sh